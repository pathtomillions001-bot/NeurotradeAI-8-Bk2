{
  "name": "@workspace/dbot-builder",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Vendored Deriv bot builder (MIT fork of deriv-com/binary-bot) + postMessage bridge. Builds the fork and syncs its static bundle into the web app at /dbot.",
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "sync": "node ./scripts/sync.mjs",
    "typecheck": "echo \"no ts sources\"",
    "test": "echo \"no unit tests — compiled output is exercised via api-server xml tests\""
  },
  "devDependencies": {}
}
